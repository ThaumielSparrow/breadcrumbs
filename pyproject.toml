[project]
name = "breadcrumbs"
version = "0.1.0"
description = "Flight session inspector from EdgeTX logs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psutil>=7.2.2",
    "pyside6>=6.10.2",
    "requests>=2.32.5",
]
